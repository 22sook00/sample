import React, { useState } from 'react'
import styled from 'styled-components'

const BreedStyle = styled.div`
position: relative;
z-index: 100000;
width : 100%;
height : 100%;
background : linear-gradient(45deg, #48cfad,#37bc9b)
`

const Breed = () => {
  return <BreedStyle>
    </BreedStyle>
}

export default Breed;